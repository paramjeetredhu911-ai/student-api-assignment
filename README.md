# Student and Course Management API

This backend API manages student registration, login validation, course creation, and student enrollment patterns.

## Environment Variables
To clone and run this application locally, create a `.env` file in the root directory and add the following keys:

PORT=3000
MONGO_URI=your_mongodb_connection_string
JWT_SECRET=yoursecretkey
